#pragma once

namespace lib 
{
    const int lib_LIBRARY_CAPACITY = 333;
    const int lib_MAX_LOAN_DAYS = 15;
    // maximum number of day a publication can be borrowed with no penalty
    const int lib_TITLE_WIDTH = 30;
    // The width in which the title of a publication should be printed on the console
    const int lib_AUTHOR_WIDTH = 15;
    // The width in which the author name of a book should be printed on the console
    const int lib_SHELF_ID_LEN = 4;
    // The width in which the shelf id of a publication should be printed on the console
//    const int lib_LIBRARY_CAPACITY = 5000;
    // Maximum number of publications the library can hold.
    const int lib_MAX_FILENAME = 256;
    // Maximum size that a filename can have
}